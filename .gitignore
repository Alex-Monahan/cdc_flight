# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.pytest-instance-locks/
.ruff_cache/
.mypy_cache/
.pytest-source.lock

# uv
# (uv.lock IS committed on purpose)

# dlt
.dlt/secrets.toml
~/.dlt/
*.duckdb
*.duckdb.wal
_storage/

# CDC runtime state (Debezium offsets, engine logs)
.cdc_state/
.cdc_instances/
logs/

# Local env
.env

# OS
.DS_Store

# Project-local Postgres cluster (disposable; recreate with `make pg-init`)
.pgdata/
.pgdata_*/
.pgdata_replica/
.cdc_state_md/

# Probe output (regenerate with `uv run python probes/pNN_*.py`)
probes/.out/

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

# Per-instance test-session mutexes (see tests/support/fixtures.py)
.pytest-source-*.lock
