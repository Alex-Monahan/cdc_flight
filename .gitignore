# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv
# (uv.lock IS committed on purpose)

# dlt
.dlt/secrets.toml
~/.dlt/
*.duckdb
*.duckdb.wal
_storage/

# CDC runtime state (Debezium offsets, engine logs)
.cdc_state/
logs/

# Local env
.env

# OS
.DS_Store

# Project-local Postgres cluster (disposable; recreate with `make pg-init`)
.pgdata/
.pgdata_replica/
.cdc_state_md/

# Probe output (regenerate with `uv run python probes/pNN_*.py`)
probes/.out/

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

# Test-session mutex for the shared :15432 cluster (see tests/conftest.py)
.pytest-source.lock
